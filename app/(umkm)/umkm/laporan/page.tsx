"use client";

import { useCallback, useEffect, useMemo, useState } from "react";
import { ArrowDownLeft, ArrowUpRight, BarChart3, CalendarCheck, CheckCircle2, Download, FileText, LoaderCircle, Pencil, Plus, Receipt, RefreshCcw, TrendingDown, TrendingUp, X, XCircle } from "lucide-react";
import { cancelLedgerTransactionClient, closeLedgerDayClient, createLedgerTransactionClient, getLedgerReportClient, updateLedgerTransactionClient } from "@/modules/ledger/ledger-client";
import { categoryOptions, jakartaDate, ledgerTransactionInputSchema, paymentMethodLabels, type LedgerTransactionInput } from "@/modules/ledger/ledger-schema";
import type { LedgerReportView, LedgerTransactionView } from "@/modules/ledger/ledger-repository";
import { MonthlyTab } from "@/components/warung/MonthlyTab";
import { ConditionTab } from "@/components/warung/ConditionTab";
import { BankReportCard } from "@/components/warung/BankReportCard";
import { ReminderStrip } from "@/components/warung/ReminderStrip";
import { InlineMoneyInput } from "@/components/warung/MoneyInput";
import { ComparisonBarChart, DashboardPage, DashboardPanel, FeedbackBanner, MetricCard, PageHeader, PanelHeader, StatusBadge, type ComparisonDatum } from "@/components/dashboard";

type Preset = "today" | "week" | "month" | "custom";
type Tab = "month" | "condition" | "bank" | "cash";
type FormState = { transactionType: "income" | "expense"; amount: string; date: string; categoryCode: string; description: string; paymentMethod: string; counterparty: string; reason: string };
function formatIdr(value: number) { return `Rp${Math.abs(value).toLocaleString("id-ID")}`; }
function dateRange(preset: Exclude<Preset, "custom">) {
  const today = jakartaDate(); const current = new Date(`${today}T12:00:00+07:00`);
  if (preset === "today") return { startDate: today, endDate: today };
  if (preset === "week") { const start = new Date(current); start.setDate(current.getDate() - 6); return { startDate: start.toLocaleDateString("en-CA", { timeZone: "Asia/Jakarta" }), endDate: today }; }
  return { startDate: `${today.slice(0, 7)}-01`, endDate: today };
}
function emptyForm(type: "income" | "expense" = "income"): FormState { return { transactionType: type, amount: "", date: jakartaDate(), categoryCode: type === "income" ? "sales_direct" : "raw_material", description: "", paymentMethod: "cash", counterparty: "", reason: "" }; }
function cashFlowData(transactions: LedgerTransactionView[]): ComparisonDatum[] {
  const daily = new Map<string, { income: number; expense: number }>();
  for (const transaction of transactions) {
    if (transaction.status === "cancelled") continue;
    const value = daily.get(transaction.transactionDate) ?? { income: 0, expense: 0 };
    value[transaction.transactionType] += transaction.amountIdr;
    daily.set(transaction.transactionDate, value);
  }
  return [...daily.entries()].sort(([a], [b]) => a.localeCompare(b)).slice(-7).map(([date, value]) => ({
    label: new Intl.DateTimeFormat("id-ID", { day: "numeric", month: "short" }).format(new Date(`${date}T12:00:00+07:00`)),
    primary: value.income,
    secondary: value.expense,
  }));
}

export default function LaporanPage() {
  const [tab, setTab] = useState<Tab>("month");
  const [range, setRange] = useState(dateRange("month")); const [preset, setPreset] = useState<Preset>("today");
  const [report, setReport] = useState<LedgerReportView | null>(null); const [loading, setLoading] = useState(true); const [busy, setBusy] = useState(false);
  const [message, setMessage] = useState<{ tone: "error" | "success" | "info"; text: string } | null>(null);
  const [form, setForm] = useState<FormState>(emptyForm()); const [editing, setEditing] = useState<LedgerTransactionView | null>(null);
  const [showTransactionForm, setShowTransactionForm] = useState(false); const [showClosing, setShowClosing] = useState(false);
  const [openingCash, setOpeningCash] = useState(""); const [physicalCash, setPhysicalCash] = useState(""); const [closingNote, setClosingNote] = useState("");
  const loadReport = useCallback(async () => { setLoading(true); try { setReport(await getLedgerReportClient(range)); setMessage((value) => value?.tone === "error" ? null : value); } catch (error) { setMessage({ tone: "error", text: error instanceof Error ? error.message : "Laporan belum dapat dimuat." }); } finally { setLoading(false); } }, [range]);
  useEffect(() => { const timer = window.setTimeout(() => void loadReport(), 0); return () => window.clearTimeout(timer); }, [loadReport]);

  const closedDates = useMemo(() => new Set(report?.closings.map((item) => item.closingDate) ?? []), [report]);
  const allowedCategories = categoryOptions.filter((option) => option.type === "both" || option.type === form.transactionType);
  const selectedCategory = categoryOptions.find((option) => option.code === form.categoryCode) ?? allowedCategories[0];
  const choosePreset = (value: Exclude<Preset, "custom">) => { setPreset(value); setRange(dateRange(value)); };
  const openCreate = () => { setEditing(null); setForm(emptyForm()); setShowTransactionForm(true); };
  const openEdit = (transaction: LedgerTransactionView) => { setEditing(transaction); setForm({ transactionType: transaction.transactionType, amount: String(transaction.amountIdr), date: transaction.transactionDate, categoryCode: categoryOptions.some((item) => item.code === transaction.categoryCode) ? transaction.categoryCode : "other", description: transaction.description, paymentMethod: transaction.paymentMethod ?? "other", counterparty: transaction.counterparty ?? "", reason: "" }); setShowTransactionForm(true); };
  const changeType = (type: "income" | "expense") => setForm((current) => ({ ...current, transactionType: type, categoryCode: type === "income" ? "sales_direct" : "raw_material" }));
  const transactionInput = (): LedgerTransactionInput => ({ transactionType: form.transactionType, amountIdr: Number(form.amount), transactionDate: form.date, categoryGroup: selectedCategory.group, categoryCode: selectedCategory.code, description: form.description, paymentMethod: form.paymentMethod as LedgerTransactionInput["paymentMethod"], counterparty: form.counterparty || null });
  const saveTransaction = async (event: React.FormEvent) => {
    event.preventDefault(); const input = ledgerTransactionInputSchema.safeParse(transactionInput());
    if (!input.success) { setMessage({ tone: "error", text: input.error.issues[0]?.message ?? "Periksa kembali transaksi." }); return; }
    if (editing && form.reason.trim().length < 3) { setMessage({ tone: "error", text: "Tuliskan alasan perubahan minimal 3 karakter." }); return; }
    setBusy(true); try { if (editing) await updateLedgerTransactionClient(editing.id, input.data, form.reason); else await createLedgerTransactionClient(input.data); setShowTransactionForm(false); setMessage({ tone: "success", text: editing ? "Perubahan transaksi disimpan dan riwayatnya dicatat." : "Transaksi berhasil disimpan." }); await loadReport(); } catch (error) { setMessage({ tone: "error", text: error instanceof Error ? error.message : "Transaksi belum berhasil disimpan." }); } finally { setBusy(false); }
  };
  const cancelTransaction = async (transaction: LedgerTransactionView) => { const reason = window.prompt("Mengapa transaksi ini dibatalkan? Riwayat transaksi tetap disimpan."); if (!reason) return; setBusy(true); try { await cancelLedgerTransactionClient(transaction.id, reason); setMessage({ tone: "success", text: "Transaksi dibatalkan. Riwayatnya tetap tersimpan." }); await loadReport(); } catch (error) { setMessage({ tone: "error", text: error instanceof Error ? error.message : "Transaksi belum dapat dibatalkan." }); } finally { setBusy(false); } };
  const closeToday = async (event: React.FormEvent) => { event.preventDefault(); setBusy(true); try { await closeLedgerDayClient({ closingDate: jakartaDate(), openingCashIdr: openingCash ? Number(openingCash) : null, physicalCashIdr: physicalCash ? Number(physicalCash) : null, note: closingNote || null }); setShowClosing(false); setMessage({ tone: "success", text: "Tutup kas hari ini selesai. Transaksi hari ini sekarang terkunci dari pengeditan." }); await loadReport(); } catch (error) { setMessage({ tone: "error", text: error instanceof Error ? error.message : "Tutup kas belum berhasil." }); } finally { setBusy(false); } };
  const todayClosing = report?.closings.find((item) => item.closingDate === jakartaDate());
  const maxCategory = Math.max(...(report?.categoryDistribution.map((item) => item.amountIdr) ?? [1]), 1);

  return <DashboardPage>
    <PageHeader title="Buku kas & laporan" description="Pahami uang masuk, biaya, dan selisih usaha dari catatan yang sudah Anda konfirmasi." icon={BarChart3} actions={<><a href={`/api/v1/ledger/export?startDate=${range.startDate}&endDate=${range.endDate}`} className="inline-flex min-h-10 items-center gap-2 rounded-xl border border-[#a9ebd0] bg-[#edfbf5] px-3 text-xs font-bold text-[#0b7a55]"><Download size={14} /> Unduh data</a><button onClick={() => setShowClosing(true)} disabled={Boolean(todayClosing)} className="inline-flex min-h-10 items-center gap-2 rounded-xl border border-[#addcf4] bg-[#eef8fd] px-3 text-xs font-bold text-[#0b5f86] disabled:cursor-not-allowed disabled:opacity-50"><CalendarCheck size={14} /> {todayClosing ? "Kas sudah ditutup" : "Tutup kas"}</button><button onClick={openCreate} className="inline-flex min-h-10 items-center gap-2 rounded-xl bg-[#0b5f86] px-3 text-xs font-bold text-white"><Plus size={14} /> Catat transaksi</button></>} />
    {message && <FeedbackBanner tone={message.tone === "error" ? "error" : message.tone} live>{message.text}</FeedbackBanner>}
    <ReminderStrip />
    <nav aria-label="Tampilan laporan" className="flex gap-1 rounded-xl border border-[#e3e9f0] bg-white p-1 shadow-[0_5px_18px_rgba(27,42,58,.04)]">
      {([{ id: "month", label: "Bulan Ini" }, { id: "condition", label: "Kondisi Usaha" }, { id: "bank", label: "Untuk Bank" }, { id: "cash", label: "Buku Kas" }] as const).map((item) =>
        <button key={item.id} onClick={() => setTab(item.id)} aria-current={tab === item.id ? "page" : undefined} className={`min-h-10 flex-1 rounded-lg px-3 text-xs font-bold transition-colors ${tab === item.id ? "bg-[#eef8fd] text-[#0b5f86] shadow-sm" : "text-[#6e859e] hover:bg-[#f3f6f9]"}`}>{item.label}</button>)}
    </nav>
    {tab === "month" && <MonthlyTab month={jakartaDate().slice(0, 7)} />}
    {tab === "condition" && <ConditionTab />}
    {tab === "bank" && <BankReportCard onOpenCondition={() => setTab("condition")} />}
    {tab === "cash" && <>
    <section className="rounded-2xl border border-[#e3e9f0] bg-white p-3 shadow-[0_8px_28px_rgba(27,42,58,.04)]"><div className="flex flex-col gap-3 md:flex-row md:items-center md:justify-between"><div className="flex gap-2 overflow-x-auto">{([{ id: "today", label: "Hari ini" }, { id: "week", label: "7 hari" }, { id: "month", label: "Bulan ini" }] as const).map((item) => <button key={item.id} onClick={() => choosePreset(item.id)} className={`whitespace-nowrap rounded-full px-3 py-1.5 text-xs font-bold ${preset === item.id ? "bg-[#0b5f86] text-white" : "bg-[#f3f6f9] text-[#4a6280]"}`}>{item.label}</button>)}</div><div className="flex items-center gap-2"><input type="date" value={range.startDate} max={range.endDate} onChange={(e) => { setPreset("custom"); setRange((r) => ({ ...r, startDate: e.target.value })); }} className="min-h-10 rounded-lg border border-[#d5dfe9] px-2 text-xs" /><span className="text-xs text-[#6e859e]">sampai</span><input type="date" value={range.endDate} min={range.startDate} max={jakartaDate()} onChange={(e) => { setPreset("custom"); setRange((r) => ({ ...r, endDate: e.target.value })); }} className="min-h-10 rounded-lg border border-[#d5dfe9] px-2 text-xs" /><button onClick={() => void loadReport()} aria-label="Muat ulang" className="rounded-lg p-2 text-[#6e859e] hover:bg-[#f3f6f9]"><RefreshCcw size={15} /></button></div></div></section>
    {loading || !report ? <div role="status" className="flex items-center justify-center gap-2 rounded-2xl bg-white p-12 text-sm text-[#6e859e]"><LoaderCircle className="animate-spin" size={18} /> Memuat buku kas...</div> : <>
      <section className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><MetricCard label="Uang masuk" value={formatIdr(report.summary.incomeIdr)} helper="Transaksi yang sudah dikonfirmasi" icon={ArrowDownLeft} tone="success" /><MetricCard label="Uang keluar" value={formatIdr(report.summary.expenseIdr)} helper="Belanja dan biaya usaha" icon={ArrowUpRight} tone="neutral" /><MetricCard label="Selisih kas" value={`${report.summary.netIdr < 0 ? "−" : ""}${formatIdr(report.summary.netIdr)}`} helper={report.summary.netIdr >= 0 ? "Kas masuk lebih besar dari kas keluar" : "Kas keluar lebih besar dari kas masuk"} icon={BarChart3} tone={report.summary.netIdr >= 0 ? "brand" : "attention"} /><MetricCard label="Hari aktif" value={`${report.summary.activityDays} hari`} helper={`${report.summary.transactionCount} transaksi aktif`} icon={FileText} tone="brand" /></section>
      {cashFlowData(report.transactions).length >= 2 && <DashboardPanel><PanelHeader title="Arus kas per hari" description="Perbandingan uang masuk dan uang keluar pada tujuh hari aktif terakhir." /><div className="p-5"><ComparisonBarChart data={cashFlowData(report.transactions)} primaryLabel="Uang masuk" secondaryLabel="Uang keluar" formatValue={formatIdr} /></div></DashboardPanel>}
      <section className="grid gap-4 lg:grid-cols-2"><Distribution title="Nilai transaksi per kategori" items={report.categoryDistribution} maximum={maxCategory} /><Distribution title="Metode pembayaran" items={report.paymentDistribution} maximum={Math.max(...report.paymentDistribution.map((item) => item.amountIdr), 1)} /></section>
      <section className="space-y-3"><div className="flex items-center justify-between"><div><h2 className="text-sm font-bold text-[#1b2a3a]">Riwayat transaksi</h2><p className="mt-1 text-[11px] leading-relaxed text-[#6e859e]">Transaksi dibatalkan tetap terlihat sebagai riwayat.</p></div><span className="shrink-0 text-[11px] font-bold text-[#6e859e]">{report.transactions.length} catatan</span></div>
        {report.transactions.length === 0 ? <div className="rounded-2xl border border-dashed border-[#c8d3de] bg-white p-10 text-center"><Receipt className="mx-auto text-[#9fb0c2]" size={30} /><p className="mt-2 text-sm font-bold text-[#1b2a3a]">Belum ada transaksi</p><button onClick={openCreate} className="mt-3 min-h-10 text-xs font-bold text-[#0b5f86]">Catat transaksi pertama</button></div> : <div className="space-y-2">{report.transactions.map((transaction) => { const locked = closedDates.has(transaction.transactionDate); return <article key={transaction.id} className={`rounded-2xl border border-[#e3e9f0] bg-white p-4 shadow-[0_8px_28px_rgba(27,42,58,.04)] ${transaction.status === "cancelled" ? "opacity-60" : ""}`}><div className="flex items-start justify-between gap-3"><div className="flex min-w-0 gap-3"><div className={`flex h-9 w-9 shrink-0 items-center justify-center rounded-xl ${transaction.transactionType === "income" ? "bg-[#edfbf5] text-[#0b7a55]" : "bg-[#fdf1f3] text-[#b4304a]"}`}>{transaction.transactionType === "income" ? <TrendingUp size={16} /> : <TrendingDown size={16} />}</div><div className="min-w-0"><h3 className={`truncate text-xs font-bold text-[#1b2a3a] ${transaction.status === "cancelled" ? "line-through" : ""}`}>{transaction.description}</h3><p className="mt-1 text-[10px] text-[#6e859e]">{transaction.transactionDate} · {transaction.categoryLabel} · {paymentMethodLabels[transaction.paymentMethod ?? "unknown"]}</p><div className="mt-1.5 flex flex-wrap gap-1.5">{locked && <StatusBadge tone="info">Kas sudah ditutup</StatusBadge>}{transaction.changeCount > 1 && <StatusBadge tone="attention">Pernah diubah</StatusBadge>}{transaction.status === "cancelled" && <StatusBadge tone="neutral">Dibatalkan</StatusBadge>}</div></div></div><div className="shrink-0 text-right"><p className={`text-sm font-bold tabular-nums ${transaction.transactionType === "income" ? "text-[#0b7a55]" : "text-[#b4304a]"}`}>{transaction.transactionType === "income" ? "+" : "-"}{formatIdr(transaction.amountIdr)}</p>{transaction.status === "confirmed" && <div className="mt-1 flex justify-end gap-1"><button onClick={() => openEdit(transaction)} disabled={locked || busy} aria-label="Ubah transaksi" className="grid h-9 w-9 place-items-center rounded-lg text-[#0b5f86] hover:bg-[#eef8fd] disabled:opacity-30"><Pencil size={13} /></button><button onClick={() => void cancelTransaction(transaction)} disabled={busy} aria-label="Batalkan transaksi" className="grid h-9 w-9 place-items-center rounded-lg text-[#b4304a] hover:bg-[#fdf1f3] disabled:opacity-30"><XCircle size={14} /></button></div>}</div></div></article>; })}</div>}
      </section>
    </>}
    </>}
    {showTransactionForm && <TransactionDialog form={form} setForm={setForm} editing={editing} busy={busy} allowedCategories={allowedCategories} onType={changeType} onClose={() => setShowTransactionForm(false)} onSubmit={saveTransaction} />}
    {showClosing && <ClosingDialog openingCash={openingCash} physicalCash={physicalCash} note={closingNote} busy={busy} setOpeningCash={setOpeningCash} setPhysicalCash={setPhysicalCash} setNote={setClosingNote} onClose={() => setShowClosing(false)} onSubmit={closeToday} />}
  </DashboardPage>;
}

function TransactionDialog({ form, setForm, editing, busy, allowedCategories, onType, onClose, onSubmit }: { form: FormState; setForm: React.Dispatch<React.SetStateAction<FormState>>; editing: LedgerTransactionView | null; busy: boolean; allowedCategories: typeof categoryOptions[number][]; onType: (type: "income" | "expense") => void; onClose: () => void; onSubmit: (event: React.FormEvent) => void }) {
  return <div className="fixed inset-0 z-50 flex items-end justify-center bg-slate-950/50 md:items-center md:p-6" role="dialog" aria-modal="true"><div className="max-h-[94vh] w-full overflow-y-auto rounded-t-3xl bg-white p-5 md:max-w-xl md:rounded-3xl md:p-6"><div className="flex justify-between"><div><p className="text-[11px] font-bold uppercase tracking-wide text-[#0b5f86]">{editing ? "Ubah transaksi" : "Catat transaksi"}</p><h2 className="mt-1 text-lg font-bold text-[#1b2a3a]">{editing ? "Perbaiki catatan" : "Uang masuk atau keluar"}</h2></div><button onClick={onClose} aria-label="Tutup"><X size={18} /></button></div><form onSubmit={onSubmit} className="mt-5 space-y-4"><div className="grid grid-cols-2 gap-2"><button type="button" onClick={() => onType("income")} className={`rounded-xl border p-3 text-xs font-bold ${form.transactionType === "income" ? "border-[#a9ebd0] bg-[#edfbf5] text-[#0b7a55]" : "border-[#e3e9f0]"}`}>Pemasukan</button><button type="button" onClick={() => onType("expense")} className={`rounded-xl border p-3 text-xs font-bold ${form.transactionType === "expense" ? "border-[#9fb0c2] bg-[#f3f6f9] text-[#34496a]" : "border-[#e3e9f0]"}`}>Pengeluaran</button></div><Field label="Keterangan"><input required value={form.description} onChange={(e) => setForm((f) => ({ ...f, description: e.target.value }))} placeholder="Contoh: Penjualan 10 porsi" className="ledger-input" /></Field><div className="grid grid-cols-2 gap-3"><Field label="Nominal"><InlineMoneyInput value={form.amount === "" ? null : Number(form.amount)} onChange={(value) => setForm((f) => ({ ...f, amount: value === null ? "" : String(value) }))} ariaLabel="Nominal transaksi" /></Field><Field label="Tanggal"><input required type="date" max={jakartaDate()} value={form.date} onChange={(e) => setForm((f) => ({ ...f, date: e.target.value }))} className="ledger-input" /></Field></div><Field label="Kategori"><select value={form.categoryCode} onChange={(e) => setForm((f) => ({ ...f, categoryCode: e.target.value }))} className="ledger-input">{allowedCategories.map((item) => <option key={item.code} value={item.code}>{item.label}</option>)}</select></Field><div className="grid grid-cols-2 gap-3"><Field label="Pembayaran"><select value={form.paymentMethod} onChange={(e) => setForm((f) => ({ ...f, paymentMethod: e.target.value }))} className="ledger-input">{Object.entries(paymentMethodLabels).filter(([key]) => key !== "unknown").map(([key, label]) => <option key={key} value={key}>{label}</option>)}</select></Field><Field label="Pelanggan / pemasok"><input value={form.counterparty} onChange={(e) => setForm((f) => ({ ...f, counterparty: e.target.value }))} className="ledger-input" /></Field></div>{editing && <Field label="Alasan perubahan"><textarea required minLength={3} value={form.reason} onChange={(e) => setForm((f) => ({ ...f, reason: e.target.value }))} placeholder="Contoh: nominal salah ketik" className="ledger-input min-h-20" /></Field>}<button disabled={busy} className="flex min-h-11 w-full items-center justify-center gap-2 rounded-xl bg-[#0b5f86] p-3 text-xs font-bold text-white disabled:opacity-50">{busy ? <LoaderCircle className="animate-spin" size={15} /> : <CheckCircle2 size={15} />}{busy ? "Menyimpan..." : "Simpan transaksi"}</button></form></div></div>;
}
function ClosingDialog({ openingCash, physicalCash, note, busy, setOpeningCash, setPhysicalCash, setNote, onClose, onSubmit }: { openingCash: string; physicalCash: string; note: string; busy: boolean; setOpeningCash: (value: string) => void; setPhysicalCash: (value: string) => void; setNote: (value: string) => void; onClose: () => void; onSubmit: (event: React.FormEvent) => void }) { return <div className="fixed inset-0 z-50 flex items-end justify-center bg-slate-950/50 md:items-center md:p-6" role="dialog" aria-modal="true"><div className="w-full rounded-t-3xl bg-white p-5 md:max-w-md md:rounded-3xl md:p-6"><div className="flex justify-between"><div><p className="text-[11px] font-bold uppercase tracking-wide text-[#0b5f86]">Tutup kas</p><h2 className="mt-1 text-lg font-bold text-[#1b2a3a]">Selesaikan catatan hari ini</h2></div><button onClick={onClose}><X size={18} /></button></div><p className="mt-2 text-xs leading-relaxed text-[#6e859e]">Boleh dilewati jika Anda belum menghitung uang fisik. Setelah ditutup, transaksi hari ini tidak dapat diedit tetapi masih dapat dibatalkan dengan alasan.</p><form onSubmit={onSubmit} className="mt-5 space-y-4"><Field label="Uang kas awal (opsional)"><InlineMoneyInput value={openingCash === "" ? null : Number(openingCash)} onChange={(value) => setOpeningCash(value === null ? "" : String(value))} ariaLabel="Uang kas awal" /></Field><Field label="Uang fisik saat ini (opsional)"><InlineMoneyInput value={physicalCash === "" ? null : Number(physicalCash)} onChange={(value) => setPhysicalCash(value === null ? "" : String(value))} ariaLabel="Uang fisik saat ini" /></Field><Field label="Catatan (opsional)"><textarea value={note} onChange={(e) => setNote(e.target.value)} className="ledger-input min-h-20" /></Field><button disabled={busy} className="min-h-11 w-full rounded-xl bg-[#0b5f86] p-3 text-xs font-bold text-white disabled:opacity-50">{busy ? "Menyimpan..." : "Tutup kas hari ini"}</button></form></div></div>; }
function Field({ label, children }: { label: string; children: React.ReactNode }) { return <label className="block text-xs font-bold text-[#1b2a3a]">{label}<div className="mt-1.5 [&_.ledger-input]:w-full [&_.ledger-input]:rounded-xl [&_.ledger-input]:border [&_.ledger-input]:border-[#d5dfe9] [&_.ledger-input]:px-3 [&_.ledger-input]:py-2.5 [&_.ledger-input]:text-sm [&_.ledger-input]:font-medium [&_.ledger-input]:outline-none focus-within:[&_.ledger-input]:border-[#0b5f86]">{children}</div></label>; }
function Distribution({ title, items, maximum }: { title: string; items: Array<{ code: string; label: string; amountIdr: number }>; maximum: number }) { return <DashboardPanel><PanelHeader title={title} /><div className="p-4 md:p-5">{items.length === 0 ? <p className="text-xs text-[#6e859e]">Belum ada data pada periode ini.</p> : <div className="space-y-3">{items.slice(0, 6).map((item) => <div key={item.code}><div className="flex justify-between gap-3 text-xs"><span className="min-w-0 truncate font-semibold text-[#4a6280]">{item.label}</span><span className="shrink-0 font-bold tabular-nums text-[#1b2a3a]">{formatIdr(item.amountIdr)}</span></div><div className="mt-1 h-2 overflow-hidden rounded-full bg-[#eef2f6]"><div className="h-full rounded-full bg-[#1590c7]" style={{ width: `${Math.max(4, Math.round(item.amountIdr / maximum * 100))}%` }} /></div></div>)}</div>}</div></DashboardPanel>; }
